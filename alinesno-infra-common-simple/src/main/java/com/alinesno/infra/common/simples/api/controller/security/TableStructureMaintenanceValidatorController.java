package com.alinesno.infra.common.simples.api.controller.security;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]表结构自动维护,自动维护数据库表结构的变更，包括新增、修改、删除表字段等操作。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class TableStructureMaintenanceValidatorController {
    private static final Logger log = LoggerFactory.getLogger(TableStructureMaintenanceValidatorController.class);

}
