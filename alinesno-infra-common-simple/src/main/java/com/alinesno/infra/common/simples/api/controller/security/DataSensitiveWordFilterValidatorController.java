package com.alinesno.infra.common.simples.api.controller.security;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]数据敏感词过滤,对数据进行敏感词过滤，防止敏感信息的泄露和不当使用。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class DataSensitiveWordFilterValidatorController {
    private static final Logger log = LoggerFactory.getLogger(DataSensitiveWordFilterValidatorController.class);

}
