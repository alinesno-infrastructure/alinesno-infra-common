package com.alinesno.infra.common.simples.api.controller.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]权限注解,通过在方法或类上添加注解，实现对接口或方法的权限控制。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class PermissionAnnotationValidatorController {
    private static final Logger log = LoggerFactory.getLogger(PermissionAnnotationValidatorController.class);

}
