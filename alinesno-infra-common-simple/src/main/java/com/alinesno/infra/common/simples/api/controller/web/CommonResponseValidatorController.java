package com.alinesno.infra.common.simples.api.controller.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]公共前端返回对象,定义和封装前端应用的公共返回对象，统一的数据结构和格式。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class CommonResponseValidatorController {
    private static final Logger log = LoggerFactory.getLogger(CommonResponseValidatorController.class);

}
