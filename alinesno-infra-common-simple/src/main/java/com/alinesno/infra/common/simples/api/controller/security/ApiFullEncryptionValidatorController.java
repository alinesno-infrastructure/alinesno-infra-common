package com.alinesno.infra.common.simples.api.controller.security;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]接口API的全加密交互,对接口API的请求和响应进行全程加密，确保数据在传输过程中的机密性和完整性。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class ApiFullEncryptionValidatorController {
    private static final Logger log = LoggerFactory.getLogger(ApiFullEncryptionValidatorController.class);

}
