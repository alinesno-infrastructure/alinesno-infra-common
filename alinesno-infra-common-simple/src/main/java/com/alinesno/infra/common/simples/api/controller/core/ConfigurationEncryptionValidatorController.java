package com.alinesno.infra.common.simples.api.controller.core;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]配置加密,对敏感配置信息进行加密处理，保护系统的安全性。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class ConfigurationEncryptionValidatorController {
    private static final Logger log = LoggerFactory.getLogger(ConfigurationEncryptionValidatorController.class);

}
