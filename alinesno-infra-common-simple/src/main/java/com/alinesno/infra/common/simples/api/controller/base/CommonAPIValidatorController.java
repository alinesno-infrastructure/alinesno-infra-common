package com.alinesno.infra.common.simples.api.controller.base;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]通用API,提供通用的API接口，供其他模块进行调用和扩展。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class CommonAPIValidatorController {
    private static final Logger log = LoggerFactory.getLogger(CommonAPIValidatorController.class);

}
