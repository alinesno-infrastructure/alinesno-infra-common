package com.alinesno.infra.common.simples.api.controller.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]黑白名单路径请求,根据请求路径进行黑名单或白名单的过滤，限制或允许特定的请求。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class PathRequestValidatorController {
    private static final Logger log = LoggerFactory.getLogger(PathRequestValidatorController.class);

}
