package com.alinesno.infra.common.simples.api.controller.core;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]分布式ID,生成分布式环境下唯一的ID，用于标识和区分不同的数据和实体。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class DistributedIdValidatorController {
    private static final Logger log = LoggerFactory.getLogger(DistributedIdValidatorController.class);

}
