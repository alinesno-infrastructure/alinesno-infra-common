package com.alinesno.infra.common.simples.api.controller.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]CORS集成,实现跨域资源共享，允许不同域名下的前端应用访问后端接口。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class CorsIntegrationValidatorController {
    private static final Logger log = LoggerFactory.getLogger(CorsIntegrationValidatorController.class);

}
