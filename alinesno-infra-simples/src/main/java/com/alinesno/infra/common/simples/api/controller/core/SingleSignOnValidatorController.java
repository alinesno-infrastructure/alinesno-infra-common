package com.alinesno.infra.common.simples.api.controller.core;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]单点登录,实现用户在多个系统中只需登录一次即可访问其他系统。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class SingleSignOnValidatorController {
    private static final Logger log = LoggerFactory.getLogger(SingleSignOnValidatorController.class);

}
