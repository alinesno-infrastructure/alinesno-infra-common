package com.alinesno.infra.common.simples.api.controller.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.web.bind.annotation.RestController;

/**
 * [功能]菜单权限集成,将菜单权限与用户角色关联，控制用户在系统中的菜单访问权限。
 * author luoxiaodong
 * version 1.0.0
 */
@RestController
public class MenuPermissionValidatorController {
    private static final Logger log = LoggerFactory.getLogger(MenuPermissionValidatorController.class);

}
